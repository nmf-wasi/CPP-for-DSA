#include <bits/stdc++.h>
using namespace std;
int main()
{
    string Lia = "Hellowww World, Sup!";
    Lia.replace(4, 0, "Wasi");
    // name of the string.replace(from what index to replace,how many index to replace, replace with what)
    cout << Lia << endl;
    return 0;
}
